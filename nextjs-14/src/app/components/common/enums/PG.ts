export const enum PG {
  // user
  USER = "/users",
  DETAIL = `/detail`,
  MYPAGE = `/mypage`,
  CHAT = `/chat`,

  BOARD = "/boards",
  POST = "/posts",

  DEMO = "/demos",
  ABOUT = "/about",
  
  PAY = "/payments",
  ITEM = "/products",

  ISSUE = "/issues"
}
